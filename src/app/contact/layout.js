export const metadata = {
  title: "Contact Us",
  description: "Get in touch with the RagRaksha team. Send us a message or reach out directly.",
};

export default function ContactLayout({ children }) {
  return children;
}
