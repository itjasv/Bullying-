export const metadata = {
  title: "Sign In",
  description: "Sign in to SafeVoice to access your personal dashboard and view your report history.",
};

export default function LoginLayout({ children }) {
  return children;
}
