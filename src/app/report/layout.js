export const metadata = {
  title: "Report Bullying",
  description: "Submit an anonymous bullying report. Choose from 6 categories, attach evidence, and create a secure passphrase to track your case.",
};

export default function ReportLayout({ children }) {
  return children;
}
