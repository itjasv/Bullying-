export const metadata = {
  title: "Track Your Report",
  description: "Enter your Report ID and passphrase to check the status of your bullying report, view the timeline, and message the admin team.",
};

export default function TrackLayout({ children }) {
  return children;
}
